syntax = "proto3";
package signaling.rpc.server;

import "github.com/aperturerobotics/bifrost/stream/srpc/server/server.proto";

// Config is the configuration for the Signaling RPC server.
message Config {
  // Server configures the peer ids and protocol ids to listen on.
  .stream.srpc.server.Config server = 1;
}
