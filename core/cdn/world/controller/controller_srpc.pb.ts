// @generated by protoc-gen-es-starpc none with parameter "target=ts,ts_nocheck=false"
// @generated from file github.com/s4wave/spacewave/core/cdn/world/controller/controller.proto (package cdn.world.controller, syntax proto3)
/* eslint-disable */

import { RefreshRequest, RefreshResponse } from './controller.pb.js'
import { MethodKind } from '@aptre/protobuf-es-lite'
import { ProtoRpc, ServerContext } from 'starpc'

/**
 * WorldRefresh invalidates a mounted CDN World's published snapshot.
 * The service is scoped by engine ID through WorldRefreshServiceID.
 *
 * @generated from service cdn.world.controller.WorldRefresh
 */
export const WorldRefreshDefinition = {
  typeName: 'cdn.world.controller.WorldRefresh',
  methods: {
    /**
     * Refresh queues a root fetch under the mount's lifetime.
     *
     * @generated from rpc cdn.world.controller.WorldRefresh.Refresh
     */
    Refresh: {
      name: 'Refresh',
      I: RefreshRequest,
      O: RefreshResponse,
      kind: MethodKind.Unary,
    },
  },
} as const

/**
 * WorldRefresh invalidates a mounted CDN World's published snapshot.
 * The service is scoped by engine ID through WorldRefreshServiceID.
 *
 * @generated from service cdn.world.controller.WorldRefresh
 */
export interface WorldRefresh {
  /**
   * Refresh queues a root fetch under the mount's lifetime.
   *
   * @generated from rpc cdn.world.controller.WorldRefresh.Refresh
   */
  Refresh(
    request: RefreshRequest,
    abortSignal?: AbortSignal,
  ): Promise<RefreshResponse>
}

/**
 * WorldRefresh invalidates a mounted CDN World's published snapshot.
 * The service is scoped by engine ID through WorldRefreshServiceID.
 *
 * @generated from service cdn.world.controller.WorldRefresh
 */
export interface WorldRefreshHandler {
  /**
   * Refresh queues a root fetch under the mount's lifetime.
   *
   * @generated from rpc cdn.world.controller.WorldRefresh.Refresh
   */
  Refresh(
    request: RefreshRequest,
    abortSignal: AbortSignal,
    context: ServerContext,
  ): Promise<RefreshResponse>
}

export const WorldRefreshServiceName = WorldRefreshDefinition.typeName

export class WorldRefreshClient implements WorldRefresh {
  private readonly rpc: ProtoRpc
  private readonly service: string
  constructor(rpc: ProtoRpc, opts?: { service?: string }) {
    this.service = opts?.service || WorldRefreshServiceName
    this.rpc = rpc
    this.Refresh = this.Refresh.bind(this)
  }
  /**
   * Refresh queues a root fetch under the mount's lifetime.
   *
   * @generated from rpc cdn.world.controller.WorldRefresh.Refresh
   */
  async Refresh(
    request: RefreshRequest,
    abortSignal?: AbortSignal,
  ): Promise<RefreshResponse> {
    const requestMsg = RefreshRequest.create(request)
    const result = await this.rpc.request(
      this.service,
      WorldRefreshDefinition.methods.Refresh.name,
      RefreshRequest.toBinary(requestMsg),
      abortSignal || undefined,
    )
    return RefreshResponse.fromBinary(result)
  }
}
