#+TITLE: Spacewave Changelog

* v0.57.4
<2026-09-07 Mon>

Spacewave 0.57.4 refreshes the loading screen, improves browser startup and
offline recovery, and makes invitations and desktop updates more reliable.

** Improvements
- Startup now shows animated artwork and clear progress through preparation,
  connection, and opening your Space. The screen respects reduced-motion
  preferences and keeps recovery actions available when loading fails.
- Browser startup downloads less data and moves more preparation work off the
  main thread.
- Local file reads and writes use less copying and perform fewer redundant
  storage operations.

** Fixes
- Browser updates preserve active sessions and recover more reliably when
  starting offline or retrying a failed download.
- Joining a Space now retains the accepted invitation state across session
  refreshes.
- Spaces added after sign-in now appear without restarting the session.
- Desktop updates preserve the installed application when replacement or
  relaunch fails, and retry temporary file-sharing conflicts on Windows.

* v0.57.3
<2026-09-04 Fri>

Spacewave 0.57.3 restores native command-line and browser build compatibility,
improves browser reliability, and reduces memory use during builds.

** Fixes
- Native command-line builds now complete successfully.
- Browser builds now find required modules reliably when using packaged
  dependencies.
- Safari and other WebKit browser sessions now handle disposable resources
  correctly.
- After a connection becomes invalid, Spacewave now refreshes affected resources
  instead of reusing stale state.

** Improvements
- Spacewave builds now use less memory, reducing failures on memory-constrained
  systems.

* v0.57.2
<2026-09-04 Fri>

Spacewave 0.57.2 adds a redesigned Keyboard Shortcuts workbench, a dedicated
Secret viewer, managed-device support for Forge Workers, and local repository
imports for Git Spaces. It also improves Notes, Drive, browser startup, invites,
device linking, session recovery, and local file storage.

** Features
- Keyboard Shortcuts now has a redesigned workbench for searching commands and
  shortcuts, recording combinations or sequences, resolving conflicts, and
  saving changes to this device, your account, or the current Space.
- Secrets now open in a dedicated viewer that shows redacted status and access
  information while keeping secret values hidden.
- Managed Devices can now join a Space as Forge Workers and reconnect after
  restart.
- Git Spaces can now import repositories from your local computer.

** Fixes
- Notes now saves edits in order, preserves drafts when a save must be retried,
  and keeps the Saved confirmation visible when an edit produces identical
  content.
- Signed-out browser sessions now recover cleanly during startup instead of
  getting stuck on stale app state.
- Drive Home, linked paths, and Back and Forward navigation now remain inside
  the current Drive and Space; newly created Drive Spaces also open at Files.
- Device links, invites, and session recovery now remain connected through
  overlapping attempts, restarts, and temporary connection failures.
- Browser connections now recover more reliably when network routes or
  connection details change.
- Dropping a file outside a Drive upload target no longer navigates away from
  Spacewave or reloads the app.
- Quickstarts now reopen an existing matching Space instead of creating a
  duplicate.
- Closing the final tab in a split now removes the empty pane instead of leaving
  a blank pane.
- New Chat channels now open to an empty conversation, and failed sends retain
  the draft for retry.
- Browser startup now reports a clear failure with Retry when an app download or
  loading step stalls.

** Improvements
- Local file storage now batches related writes more efficiently, improving save
  performance and durability in browser sessions.
- Returning users and direct links now show the loading screen immediately
  instead of briefly showing the signed-out landing page.
- Terminal connection failures now show actionable guidance, and Retry
  reconnects with a clean session.
- Managed Device setup now offers clearer setup, sign-in, approval, and recovery
  steps.
- Release pages now distinguish the latest release from the selected release and
  show download details before installation.
- Large browser sessions keep file-storage cache growth bounded and load nearby
  file data with fewer reads.

** Security
- Spaces created in the browser now protect locally stored content with
  authenticated encryption. Spaces created by earlier browser versions remain
  readable but must be migrated before editing.
- Browser sign-in completion links are tied to the browser session that started
  sign-in.
- Space sync rejects updates from peers that no longer have access.

* v0.57.1
<2026-08-29 Sat>

Spacewave 0.57.1 adds a redesigned Keyboard Shortcuts workbench, a dedicated
Secret viewer, and managed-device support for Forge Workers. It also improves
browser startup, Drive navigation, Quickstarts, split layouts, invites and
device linking, session recovery, Terminal, Forge workflows, and local file
storage, with stronger protection for browser sign-in and saved content.

** Features
- Keyboard Shortcuts now has a redesigned workbench for searching commands and
  shortcuts, recording key combinations or sequences, resolving conflicts, and
  saving changes to this device, your account, or the current Space.
- Secrets now open in a dedicated viewer that shows redacted status and access
  information while keeping secret values hidden.
- Managed Devices can now join a Space as Forge Workers and reconnect after
  restart.

** Fixes
- Dropping a file outside a Drive upload target no longer navigates away from
  Spacewave or reloads the app.
- Drive Home, linked paths, and Back and Forward navigation now stay inside the
  current Drive and Space.
- Quickstarts now reopen an existing matching Space instead of creating a
  duplicate and running setup over its contents.
- Closing the final tab in a split now removes the empty pane instead of leaving
  a blank pane or producing duplicate-tab layout errors.
- New Chat channels now open to an empty conversation instead of loading
  forever, and a failed send keeps the draft available for retry.
- Local file storage no longer omits a newly created file when the same save
  also updates another file.
- Session setup, updates, and removal now recover automatically when another tab
  or process changes local file storage.
- Device links and invites now retain their connection and sync after
  acceptance, restart, or reconnection instead of dropping shortly afterward.
- Forge Cluster creation now accepts valid default names, reports failed or
  empty data clearly, and prevents Job setup from getting stuck while loading
  Clusters.
- Browser startup now reports a clear failure with Retry if an app download or
  loading step stalls, rather than leaving the progress screen indefinitely.

** Improvements
- Returning users and direct links now show the loading screen immediately
  instead of briefly showing the signed-out landing page.
- Space rows now open reliably, use a separate copy action, and keep closed Join
  and Link dialogs from resurfacing stale progress or results.
- Terminal connection failures now show safe, actionable guidance, and Retry
  reconnects with a clean session.
- Managed Device setup now offers a direct setup command or an install-and-run
  option, with a clearer path to sign-in and approval.
- Documentation navigation is denser and clearer on narrow screens, with the
  current page kept visible and duplicate title bars removed.
- Release pages now offer explicit Latest release and This release choices and
  show the platform, architecture, file type, and version before a desktop
  download.
- Large browser sessions now keep file-storage cache growth bounded and load
  nearby file data with fewer reads.

** Security
- Spaces created in the browser now protect locally stored content with
  authenticated encryption. Spaces created by earlier browser versions remain
  readable but must be migrated before they can be edited.
- Browser sign-in completion links are now tied to the browser session that
  started sign-in, so a copied link cannot complete the flow elsewhere.
- Space sync now rejects updates from peers that no longer have access.

* v0.57.0
<2026-07-31 Fri>

Spacewave 0.57.0 makes browser tabs, updates, local file storage, session
recovery, and large Spaces more reliable. It also fixes Space exports and Drive
split layouts, clarifies Storage Health, and improves Documentation.

** Features
- Forge tasks can now retry failed attempts while preserving their session
  context and inputs.

** Fixes
- Space exports now download correctly in browser sessions instead of returning
  a missing-file error, and opening files or exports no longer depends on the
  Space already being open elsewhere.
- Dragging a Drive file into a right-side split now creates only the requested
  pane, without opening extra Drive Quickstarts or creating unintended Spaces.
- Creating or collapsing a split layout no longer reloads the Space or cancels
  an upload already in progress.
- Opening a second Chrome tab no longer gets stuck while connecting, and each
  browser tab now keeps its own active URL while sharing the Spacewave tab list.
- Browser file storage now keeps saved data consistent while another tab writes,
  preventing concurrent browser use from making files or app data appear
  missing.
- Account setup Back now returns to the page where setup began instead of
  reopening the cloud sign-in step.
- Session recovery and device linking now handle overlapping connection attempts
  reliably, reducing stalls after sign-in, unlock, or reconnect; a later retry
  also keeps a completed device link instead of starting over.
- Live Space views now wake for every saved change instead of sometimes staying
  stale until another edit.

** Improvements
- Storage Health now separates browser usage, device storage, and sync activity,
  warns only after an actual save failure, and refreshes cleanup protection
  immediately after you request it.
- Browser updates now tie cached app files to the active release and revalidate
  them, reducing stale or mixed-version views after an update.
- Local saves now recover automatically from temporary storage conflicts,
  reducing failed saves during concurrent activity.
- Large Spaces are less likely to become unresponsive during storage cleanup,
  which now saves progress in smaller steps.
- Documentation now opens in the active split without replacing your layout.
  Search covers user and self-hoster guides, ranks title matches first, supports
  keyboard selection, and uses clearer controls in narrow panes.

** Security
- Device linking now grants access only after both devices confirm the same
  verification code for that exact link.

* v0.56.1
<2026-07-18 Sat>

Spacewave 0.56.1 adds new Canvas tools, Storage Health and recovery guidance,
contextual browser tab titles, and version-specific release pages. It also
improves Drive uploads, invites, device linking, multi-tab browser use, Terminal
continuity, experimental V86 VM setup, and Windows desktop startup.

** Features
- Canvas now includes line, arrow, rectangle, and ellipse tools, plus an image
  picker for placing pictures from Drive.
- Session settings now include Storage Health, showing browser usage, cleanup
  protection, sync activity, and recovery options for freeing space, linking
  another device, using Spacewave Cloud, or exporting important Spaces.
- Dragging several selected Drive items into a layout now opens one Spacewave
  tab per item, in selection order.
- The Command Palette now includes Select Account for switching directly between
  available accounts.
- Each changelog release now has its own page with desktop downloads pinned to
  that version.

** Fixes
- Large Drive selections now upload up to three items at a time and publish each
  completed file immediately; one failed file no longer stops the rest of the
  selection.
- When browser file storage is full, Drive now shows capacity and recovery
  guidance and removes incomplete data instead of consuming more space on
  repeated failures.
- Invite links opened without an active session now survive sign-in and session
  setup, then open the shared Space after acceptance.
- Fixed multiple browser tabs and popouts so their Spacewave tab list stays in
  sync while each page keeps its own active tab, URL, and layout.
- Terminal command sessions now survive pane remounts when a split layout
  collapses, instead of ending with a session failure.
- Fixed Windows desktop startup so packaged releases open Spacewave normally
  without stray console windows.
- When a Space has no configured home object, Spacewave now shows its object
  list instead of an empty view.

** Improvements
- Browser tab titles now follow the active section, Space, object, selected
  viewer, or focused layout pane, making tabs and bookmarks easier to identify.
- Device linking now lets you click the code to copy it, shows ordered
  connection progress, and surfaces direct-link failures instead of leaving an
  indefinite spinner.
- Experimental V86 VM setup now stays in its wizard and reports real image-copy
  and creation progress, with Retry for recoverable failures.
- Terminal now shows a Connecting state while it opens, and Command Line output
  renders more consistently across pane changes.
- Browser file storage now requests protection from automatic cleanup after the
  first successful save, reducing eviction risk without delaying startup.
- Browser startup now waits until app features are fully ready before leaving
  the loading flow, reducing blank or partially initialized views after updates.

** Security
- Password and backup recovery keys are now separate actions, so generating a
  backup key no longer also adds the password entered for the session.

* v0.56.0
<2026-07-10 Fri>

Spacewave 0.56.0 makes cold browser starts faster by making startup checksum
processing 55.7× faster and eliminating the largest measured cold-boot
bottleneck. It also removes repeated file-storage integrity work, restores
Terminal in browser builds, and reduces byte-copying during browser startup and
data loading.

** Fixes
- Fixed Terminal in browser builds so it opens and reaches the command prompt
  again.
- Object details now includes a permission-aware Danger Zone for permanently
  deleting the selected object with explicit confirmation.
- Keyboard shortcut recording now captures key combinations even after focus
  moves away from the recorder, and Escape or clicking away cancels recording.

** Improvements
- Cold browser starts are faster: startup checksum processing is 55.7× faster,
  eliminating the largest measured cold-boot bottleneck.
- Local file storage now avoids duplicate integrity work while preserving
  compatibility with existing stored data, reducing repeated work during startup
  and file access.
- The shortcut editor now shows held modifiers while recording, formats captured
  keys like application menus, and identifies conflicting commands with a direct
  replace action.
- Browser startup and in-app data loading now avoid an unnecessary byte
  conversion, reducing copying and memory pressure.
- Release checks now verify existing local file storage compatibility and
  confirm that browser builds include Terminal, reducing the chance of storage
  or Terminal regressions.

* v0.55.0
<2026-07-10 Fri>

Spacewave 0.55.0 completes the on-demand SQL Database tools work behind the
smaller browser startup download. It also restores the desktop Command Line
terminal, serves the app shell before browser development startup checks finish,
adds Space type glyphs to session lists, and makes developer builds more
reliable under load.

** Features
- Session Space lists now show each Space's registered type glyph, making Drive,
  Git Spaces, and other Space types easier to identify.

** Fixes
- Browser development builds now serve the Spacewave app shell before plugin
  startup checks finish, so the page can show useful status and errors instead
  of remaining blocked.
- Fixed File → Open CLI Terminal in desktop builds so the Command Line prompt
  appears and commands run instead of leaving a blank Terminal.
- Fixed Command Palette searches so selecting all filter text and deleting it
  clears the entire search instead of a single character.
- Fixed local CLI connections for custom data directories so Command Line tools
  select the running Spacewave instance from the active data location.
- Fixed public blog pages so author avatars and complete styling load reliably,
  and corrected the launch post's closing-text formatting.

** Improvements
- Completed the on-demand SQL Database tools work, preserving the smaller
  browser startup download while keeping SQL tools reliable when loaded.
- Spacewave code generation now works with TypeScript 7 in fresh developer
  installs.
- Plugin builds now limit concurrent work, making Spacewave builds more reliable
  under heavy load and on memory-constrained machines.

* v0.54.0
<2026-07-09 Thu>

Spacewave 0.54.0 makes the first browser load dramatically faster: the core
startup download is about 90% smaller, with SQL Database tools loaded only when
you open them. It also adds honest startup download progress, a branded loading
screen, richer Create Object choices, blog subscriptions, and broad reliability
fixes for browser updates, Quickstarts, Drive, desktop startup, and peer-to-peer
connections.

** Features
- Create Object now shows icons and one-line descriptions, includes registered
  Chat Channel, Documentation, Blog, Forge Dashboard, and V86 VM choices, and
  reliably refreshes the available choices when the palette opens.
- The Spacewave blog now lets readers subscribe for development updates and
  release announcements directly from the end of a post.
- Command Line can now open in a Spacewave tab, with help, command history,
  interrupt, and live watch support.
- SQL Database saved queries now restore their parameters, including binary
  values, and supported table views can update rows safely within the active
  view.
- System Status now includes a Network panel showing live connection and
  transport state.

** Fixes
- Fixed browser startup paths that could stop at a completed download, wedge
  after a reload, or fail to continue when a replacement browser tab was already
  ready.
- Fixed browser updates and reloads that could reuse stale app downloads,
  reducing stuck loading screens and stale views after a new release.
- Fixed desktop startup failures that could leave the app unable to connect to
  its window or repeatedly retry a valid app screen.
- Fixed the Home tab after Drive Quickstart so it opens the session instead of
  showing a blank grey pane.
- Fixed Drive drag-and-drop so files can be moved onto folders, root and parent
  breadcrumbs, and Up controls without the drop silently disappearing.
- Fixed Notebook, Docs, and Blog Quickstarts so their app content can load on
  demand and open the seeded object; Forge Dashboard and V86 VM startup paths
  also recover more reliably.
- Fixed WebRTC connection teardown so pending channels close cleanly when the
  browser connection disappears instead of leaving stalled peer-to-peer work
  behind.
- Fixed browser-based numeric operations for 32-bit multiplication and string
  minimum/maximum comparisons, preventing incorrect results in affected apps.

** Improvements
- The browser's core startup download is about 90% smaller because SQL Database
  tools are no longer part of every first load; shared app code is also cached
  more effectively across reloads and updates.
- Browser startup now appears immediately with Spacewave branding, clear phases,
  live byte counts, per-download progress, and Retry or Back actions when
  startup fails.
- Drive uploads now show start and completion feedback near the upload
  indicator, and first-run guidance presents one callout at a time without
  hiding upload progress.
- Git Spaces bring in large repositories more efficiently, reducing storage
  overhead during clone and import operations.
- Docs now adapt more cleanly to narrow windows and smaller screens.
- Desktop development builds start faster by skipping browser-only startup work
  that the desktop app does not need.

** Security
- Updated cryptographic dependencies to address reported security issues.

* v0.53.1
<2026-07-05 Sun>

Spacewave 0.53.1 is a reliability patch for browser startup and local file
storage. It helps older browser profiles recover from stale local app cache data
and fixes a rare storage stall in ephemeral and browser sessions.

** Fixes
- Fixed browser startup for profiles created on older Spacewave versions. When
  old local app cache data uses a hash format the current browser build cannot
  read, Spacewave now resets that cache automatically and starts fresh instead
  of staying on the loading screen.
- Fixed a rare permanent stall in in-memory file storage where a cancelled write
  could leave later reads blocked, affecting ephemeral and browser sessions.

** Improvements
- Browser sessions now switch context more smoothly under load, reducing cases
  where busy work could delay other browser activity.

* v0.53.0
<2026-07-04 Sat>

Spacewave 0.53.0 adds editable keyboard shortcuts, richer Command Palette
discovery, and Org file support for notes and docs. It also improves Drive
Quickstart reliability, browser startup, session recovery, desktop shortcut
handling, and object-heavy Space responsiveness.

** Features
- Keyboard Shortcuts can now be edited from Tools, account settings, and Space
  settings, with Local, Account, and Space scopes, conflict warnings, reset
  actions, and custom leader-key settings.
- Command Palette now combines command search and key-sequence discovery:
  Cmd/Ctrl+K opens the palette, typing filters commands, and key paths show the
  next available actions.
- Notebooks and Docs now recognize Org files. You can create Org notes, open
  index.org docs pages, and edit supported Org content in WYSIWYG or source mode
  while preserving Org metadata.

** Fixes
- Fixed a Drive Quickstart stall where command setup could loop after the Space
  was created, leaving a blank Space view instead of the file browser.
- Fixed Quickstart wizard name fields so clearing a suggested name and typing a
  replacement no longer restores or appends the old name.
- Fixed local session recovery after unlock or reconnect so Spacewave retries
  the current session connection instead of waiting on one that was already
  replaced.
- Fixed browser startup paths that could leave some Notebooks, Blog, and Forge
  views stuck before the view appeared after browser updates.
- Fixed desktop keyboard shortcuts so native menu accelerators no longer take
  over Command Palette and custom shortcut keys before Spacewave can handle
  them.

** Improvements
- Drive Quickstart, Space creation, and object-heavy views do fewer duplicate
  storage lookups and writes, improving responsiveness during creation and
  navigation.
- Command Palette result matching and selection are more predictable, with
  highlighted alias matches and clearer shortcut and conflict hints.
- Keyboard shortcut lists, menus, and Command Palette rows now show the
  effective shortcuts after Local, Account, or Space overrides.
- Browser startup after updates is less likely to be blocked by stale local
  startup state, reducing stuck loading screens.
- Browser sessions start faster and use less memory, keeping Drive, object
  views, and Quickstart flows more responsive during startup and navigation.

** Security
- Experimental VM file storage now rejects oversized filesystem strings instead
  of crashing the VM file bridge, reducing an availability risk in VM file
  sharing.

* v0.52.0
<2026-07-02 Thu>

Spacewave 0.52.0 improves Drive file storage, browser startup and browser
updates, Command Line setup, and experimental data workspaces. This release also
makes large uploads, media previews, Canvas drawing, Git Spaces, and SSH Host
connections more reliable.

** Features
- Experimental Key/Value Store Quickstart now creates a seeded data workspace
  with a dedicated Key/Value Store viewer for browsing, filtering, creating,
  editing, and deleting keys.
- Experimental SQL Database Quickstart now creates a seeded SQL workspace with
  database, schema, query, result, table view, and SQL Workbench viewers.
- The desktop app can now guide Command Line install and update actions, show
  the selected install target, and provide copyable commands for the active
  session.
- New Drive Spaces now open with a getting-started banner in the file browser,
  with quick actions for uploading files, creating folders, opening the starter
  guide, and inviting people when sharing is available.

** Fixes
- Fixed Drive files with spaces in their names so inline previews and downloads
  open correctly instead of returning missing-file errors.
- Fixed Drive video previews so scrubbing and seeking in uploaded videos works
  reliably in browser sessions.
- Fixed large Drive drag-and-drop uploads so multi-file uploads complete with
  lower memory pressure and fewer stalls.
- Fixed Command Line status on first run so it no longer hangs silently while a
  session is mounting; it now returns useful status or a bounded startup
  diagnostic.
- Fixed Canvas drawing so rapid pen strokes stay visible while writes and synced
  state catch up.
- Fixed Git Spaces with empty repositories so they show an empty-repository
  state instead of trying to open missing files or logs.
- Fixed browser update recovery when local cached app files cannot be written,
  reducing partial updates, unhandled startup errors, and stuck loading states.

** Improvements
- Browser startup and first-run Drive setup do less blocking file-storage work,
  reducing time spent on initial loading screens in common fresh-start paths.
- Shell tabs, split layouts, menu overlays, and bottom bars handle narrow panes,
  restored tabs, and overflow actions more predictably.
- Device and SSH Host setup forms now use clearer fields for host, port, user,
  credentials, and setup target.
- Command Line setup now explains the active session, local socket state,
  install guidance, and next useful commands more clearly.
- Drive and file viewers include better loading and recovery behavior for file
  lookup, previews, and refreshed file lists.

** Security
- Terminal now asks you to review and trust an SSH Host key before connecting,
  and remembers accepted keys for that Host.
- Diagnostic logs avoid including private object identifiers, reducing
  accidental exposure in support logs.

* v0.51.8
<2026-05-31 Sun>

Spacewave 0.51.8 improves browser and desktop update recovery, Drive and
first-run polish, billing visibility, desktop tray behavior, and file gallery
refreshes. It also adds experimental Computers, SSH Host, and Terminal surfaces
for early device workflows.

** Features
- Billing usage now warns when storage, write operations, or Cloud Reads
  approach included usage, and shows clearer storage overage estimates.
- Experimental Computers workspaces can now guide Add Device setup, list Devices
  and SSH Hosts, and open Terminal tabs when terminal access is available.

** Fixes
- Fixed stale browser and desktop update state so Spacewave is less likely to
  open a blank or broken app after an update.
- Fixed Drive Gallery so image folders update live when files are added,
  removed, or renamed.
- Fixed stale split/grid layout tabs so old restored layouts no longer render
  missing tabs or fall back to the wrong view.
- Fixed first-run Get Started icon sizing and app font scaling so actions and
  tabs stay readable in narrow layouts or with larger system text.
- Fixed crowded bottom bars so overflow actions collapse into a menu instead of
  squeezing upload progress, breadcrumbs, or viewer controls.
- Fixed desktop tray/menu bar submenu and keyboard-focus edge cases so tray
  actions stay reachable.

** Improvements
- The Drive landing page, demo, and Quickstart copy now explain private browser
  files, offline work, device sync, and optional cloud backup more clearly.
- Empty session dashboards now prioritize Create a Drive, blank Space creation,
  device linking, and invite joining more clearly.
- Object trees and file rows have cleaner spacing and alignment for easier
  scanning.
- Command Line status now includes clearer app recovery details for support when
  startup or update recovery fails.
- Desktop tray/menu bar presentation handles long labels, Session and Space
  navigation, update actions, and fallback menus more reliably.

** Security
- SSH Host setup keeps private keys, passwords, and passphrases in Secrets and
  shows only credential-link status in Host, Device, Terminal, and Computers
  views.

* v0.51.7
<2026-05-29 Fri>

Spacewave 0.51.7 improves the first-run Drive path, browser startup, local file
storage, and app documentation. New Drive Spaces guide users into the file
browser more clearly, returning browser sessions show better startup progress
and recovery, and large file operations are more reliable across reloads,
browser updates, and multi-tab use.

** Features
- Drive Quickstart now opens with a Drive intro step and starter guide, then
  takes you into the file browser when you choose Open files.
- The desktop app now includes standard Close Window and Quit commands.

** Fixes
- Fixed Drive Quickstart paths that could hang before the first file list
  appeared, especially in browser sessions.
- Fixed multi-tab browser startup so a second Spacewave tab can reach the app
  instead of staying on the loading screen.
- Fixed sign-in redirects so stale browser sign-in pages do not restart the
  wrong SSO flow or loop back into sign-in.
- Fixed large Drive uploads, directory reads, and file previews in browser
  sessions so they are less likely to stall or fail after the page opens.
- Fixed Git commit diffs so very large patches are bounded and still show the
  available file changes.
- Fixed Blog editing so saved posts refresh and source edits wait until the file
  is ready.

** Improvements
- Returning browser sessions now show phase-by-phase startup progress with
  clearer retry and back actions when startup fails.
- Browser startup and browser updates load app files more efficiently, reducing
  blank or slow first screens after reloads.
- Browser file storage now does large upload and sync work with lower memory
  pressure.
- The setup banner waits until a local session contains meaningful stored data
  before prompting users to finish account setup.
- The in-app docs have been rewritten around Users, Self-Hosters, and
  Developers, with clearer starting paths for Drive, Spaces, accounts, storage,
  Command Line, plugins, and SDK work.
- Session setup, link-device, local setup, and cloud setup flows now stay inside
  the session frame with a consistent Back action.

** Security
- Public blog and static pages now handle embedded page data more safely when
  opening in the browser.

* v0.51.6
<2026-05-11 Mon>

Spacewave 0.51.6 is a reliability release for setup, Quickstart, browser
startup, and public pages. Local setup progress is less likely to get lost
during startup, plugin-backed Spaces recover more reliably after reloads or
browser updates, and public blog/video pages behave better across browsers and
theme extensions.

** Fixes
- Fixed local setup and Quickstart routing so Spacewave is less likely to stay
  on the plan page, reopen the wrong setup step, or let an inactive split-view
  panel change the active route.
- Fixed browser startup for plugin-backed Spaces so already-loaded app files are
  reused during startup, reducing blank shells and failed app loads after
  reloads or browser updates.
- Fixed the About dialog so it stays centered and visible in the app window.
- Fixed Spacewave blog video embeds in Firefox and other isolated browsers by
  showing a safe Watch on YouTube link when inline playback is blocked.
- Fixed browser theme-extension conflicts so Dark Reader does not restyle
  Spacewave into unreadable colors.

** Improvements
- Local file storage does less background work while Spaces open, reducing
  delays before local Spaces become ready.
- Object views in large Spaces can load only the linked items they need,
  reducing stalls in relationship-heavy views.
- Browser tabs with several plugin-backed viewers use lighter startup
  communication, reducing startup failures when many viewers are active.
- Published release notes now include direct desktop download links for macOS,
  Windows, and Linux.

* v0.51.5
<2026-05-09 Sat>

Spacewave 0.51.5 is a reliability release for browser startup, local setup,
Drive, and session cleanup. Sign-in and local onboarding recover more reliably,
plugin-backed Spaces are less likely to get stuck after reloads or browser
updates, and Drive/file storage edge cases now produce fewer missing or stalled
views.

** Features
- Command Line commands now accept plural aliases for Spaces, Canvases, and VMs,
  matching the nouns users see in help and docs.

** Fixes
- Fixed sign-in and local setup startup so Spacewave is less likely to stay on a
  static shell or blank page before the setup wizard appears.
- Fixed browser startup for plugin-backed Spaces so missing app assets or
  unready app workers are retried instead of leaving the page stuck.
- Fixed Drive Quickstart and Drive views where bottom bar updates could crash
  the page or prevent uploads and video previews from appearing.
- Fixed logout and session close handling so local sign-out, stale-session
  removal, and account-session revocation report the right state and close
  cleanly.
- Fixed local Space opening so Spacewave waits for local file storage to be
  ready before continuing, reducing session recovery and settings loading races.
- Fixed browser file storage edge cases that could hide matching entries from
  folder/path views or leave nested copied content incomplete.
- Fixed a Windows desktop build issue that could block the app from starting on
  that platform.

** Improvements
- Browser startup and hard reloads now recover more reliably when app code is
  still loading or needs to be recreated.
- Command Line state-path commands can start the local Spacewave runtime
  automatically, so status, session, Space, and sign-out commands reach useful
  session state instead of stopping at a missing daemon.
- Browser update recovery now includes more complete app content during rollout,
  reducing broken or incomplete loads after an update.

* v0.51.4
<2026-05-09 Sat>

Spacewave 0.51.4 is a reliability release for browser and desktop startup.
Updates recover more reliably during rollout, browser tabs resume with fewer
stale views, and cold browser loads do less work before the first useful screen
appears.

** Fixes
- Fixed update checks so the desktop app keeps retrying when release information
  is not ready at launch, instead of leaving update status stuck on an old
  error.
- Fixed release loading so Spacewave can recover when local update files are
  incomplete, reducing blank or stuck loading screens during a rollout.
- Fixed restored or foregrounded browser tabs so reconnect timing waits until
  the page is ready, reducing stale views and unnecessary app reloads.
- Fixed updater status for releases that do not include a desktop app update for
  the current platform, so old update errors are cleared.

** Improvements
- Browser startup now loads less of the app before showing the first relevant
  screen, improving cold-load performance for sign-in, Quickstart, docs, and
  active sessions.
- Browser resume handling now distinguishes a slow foregrounded tab from a
  broken connection, making return-to-tab recovery less disruptive.
- Release file recovery now reuses verified local files after a successful
  fetch, reducing repeat work on later starts.

* v0.51.3
<2026-05-08 Fri>

Spacewave 0.51.3 improves desktop and browser startup/update reliability, adds a
tray/menu-bar update action, and fixes rough edges in Cloud setup, Drive video
previews, and session recovery status.

** Features
- The desktop tray/menu bar now shows an Install Update action when a Spacewave
  desktop update is ready.

** Fixes
- Fixed a desktop startup problem where Spacewave could keep running without
  showing a window after an update.
- Fixed Spacewave Cloud setup and billing actions in split views so checkout,
  cancellation, and reactivation stay in the active panel instead of jumping to
  the wrong session page.
- Fixed browser updates and root-page reloads so fresh tabs are less likely to
  show an old or blank Spacewave page.
- Fixed Drive video previews so restored video tabs and back/forward navigation
  return to playable previews.
- Fixed the desktop tray Settings action so it opens Command Line settings for
  the active session.

** Improvements
- Browser startup can show the app frame earlier, reducing time spent on the
  initial loading screen during cold web loads.
- Session recovery status now looks the same in the app and the desktop tray,
  including pending Spaces, unlock prompts, retries, and failures.
- Command Line guidance now reflects the tray/menu-bar runtime, making it
  clearer when CLI commands can connect after all Spacewave windows are closed.

* v0.51.2
<2026-05-07 Thu>

Spacewave 0.51.2 makes Drive workspaces, sharing, and desktop use easier while
improving sign-in, session recovery, browser updates, and local file storage
reliability.

** Features
- Drive workspaces now open directly into a file browser for folders, uploads,
  downloads, and organization.
- You can copy a public link to the current view from the app, making it easier
  to share the exact Spacewave page you are using.
- Space sharing can now invite someone by Spacewave username, with pending
  invites shown in the app for accept or decline.
- The desktop app now has a native desktop tray or menu bar item for reopening
  Spacewave, checking sessions and Spaces, and quitting cleanly.
- The desktop app can add and open existing local Spacewave data directories
  from the sign-in and session screens.

** Fixes
- Fixed invite links copied from the desktop app so recipients open the shared
  Space in Spacewave correctly.
- Fixed newly created Spaces disappearing from the Space list after refresh.
- Fixed browser startup paths that could leave the app stuck on the loading
  screen after opening account emails.
- Fixed desktop launch and quit behavior so the app no longer reopens
  unexpectedly or stalls before showing a window after an update.
- Fixed desktop file pickers so hidden local Spacewave folders can be selected
  when needed.
- Fixed the Space details object list so it updates while the details panel
  stays open.
- Fixed Space deletion so the confirmation dialog stays populated and the
  visible delete step completes faster.
- Download and other off-site links now open in a separate tab instead of
  replacing the running Spacewave page.

** Improvements
- Sign-in now shows clearer in-progress feedback while passkey, browser, or SSO
  authentication is running.
- Session recovery after sign-in now runs in the background, with a compact
  status indicator and unlock prompts only when a Space needs your account key.
- Browser updates are more reliable when tabs reload, regain focus, or come back
  online, reducing stale or mixed app versions after a release.
- Browser file storage is more durable after interrupted writes or restored
  tabs, reducing stale file views and startup failures.
- Drive and file browser controls are clearer, with improved toolbar actions,
  selected-row states, retryable file loading errors, and better
  unavailable-preview messages.
- The desktop tray shows session, Space, update, and attention status even when
  all windows are closed.

** Security
- Username-addressed invites are tied to the intended Spacewave account and
  require recipient acceptance before access is granted.

* v0.51.0 Public Launch
<2026-05-01 Fri>

Spacewave is open-source, private, self-hosted, multiplayer Spaces (apps) that
store data wherever you want, work offline through peer-to-peer networking, and
sync instantly with real-time streaming multiplayer. v0.51 is the public launch:
a suite of first-party Spaces, a plugin SDK to build your own, and desktop, web,
and CLI clients, all Apache-2.0 licensed. Try it at [[https://spacewave.app][spacewave.app]] in one click
with no account required, read the [[https://spacewave.app/blog/2026/04/launch][launch announcement]], grab the code at
[[https://github.com/s4wave/spacewave][github.com/s4wave/spacewave]], or [[https://discord.gg/KJutMESRsT][join the community on Discord]] and let us know
what you want added next.

** Features

- Spaces: shared multiplayer workspaces. Add your friends or coworkers and
  collaborate live, with role-based access and invite flows.
- Drive: store and access your files. Drag-and-drop upload, projected downloads,
  zip export, and live file watching across linked devices.
- Notes: write and collaborate with Markdown text files. Offline-first, with
  inline previews and live editing.
- Chat: communicate securely with humans or robots. End-to-end encrypted and
  synced across linked devices, online or offline.
- Canvas: an infinite 2D workspace for visual thinking, with embedded live
  objects from any other Space.
- Git: browse repositories with branches, tags, commit history, and tree views.
  No clone or terminal needed.
- Media: photo gallery and inline image previews wherever files appear in the
  app.
- Plugin SDK: write your own Spaces in TypeScript or Go with hot reload, or
  install plugins from any Git repo. Same APIs the first-party plugins use.
- Cross-platform: desktop apps for macOS, Windows, and Linux, a one-click web
  app, and a CLI, all built from a single Go and TypeScript codebase.
- Account and session management: passkeys, account recovery, multi-device
  session linking, and session self-rejoin.
- Spacewave Cloud (optional): encrypted 100GB backup for $8/mo. Private, secure,
  fast (via Cloudflare), and dead simple. The app works fine without it.

** Security

- End-to-end encrypted storage and messaging across all linked devices. Keys
  live on your devices and never leave them unless you explicitly export a
  backup.
- Spacewave Cloud backups are encrypted on your device before they are uploaded.
  The cloud only ever sees encrypted data.
