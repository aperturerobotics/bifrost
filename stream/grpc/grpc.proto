syntax = "proto3";
package stream.grpc;

// Request is a request packet.
message Request {
  // Data is packet data from the remote.
  bytes data = 1;
}

// Response is a response packet.
message Response {
  // Data is packet data from the remote.
  bytes data = 1;
}