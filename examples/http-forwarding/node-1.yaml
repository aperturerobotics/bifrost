udp:
  config:
    dialers: {}
    listenAddr: :5112
    packetOpts:
      blockCompress: BlockCompress_NONE
      blockCrypt: BlockCrypt_NONE
      # blockCompress: BlockCompress_SNAPPY
      # blockCrypt: BlockCrypt_AES256
      kcpMode: KCPMode_FAST3
  id: bifrost/udp/1
  revision: 1
forwarding:
  config:
    protocolId: my/http/forwarding/1
    targetMultiaddr: "/ip4/127.0.0.1/tcp/8080"
  id: bifrost/stream/forwarding/1
  revision: 1

