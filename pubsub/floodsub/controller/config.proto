syntax = "proto3";
package floodsub.controller;

import "github.com/aperturerobotics/bifrost/pubsub/floodsub/floodsub.proto";

// Config is the floodsub controller config.
message Config {
  // FloodsubConfig are mesh-sub specific configuration variables.
  floodsub.Config floodsub_config = 1;
}